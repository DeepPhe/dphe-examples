# dphe-examples
Example data for DeepPhe.  Text Documents, Timeline CSVs, OMOP JSONs, etc.

The maven project will build an empty jar in addition to creating a base resources/ directory with the examples.